using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IsACell : MonoBehaviour
{
    public bool isHoldingACell;
    // Start is called before the first frame update
    void Start()
    {
        isHoldingACell = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
